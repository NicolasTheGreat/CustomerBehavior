import { MarkerOffsetPipe } from './marker-offset.pipe';

describe('MarkerOffsetPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkerOffsetPipe();
    expect(pipe).toBeTruthy();
  });
});
